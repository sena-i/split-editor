rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Split Editor
    match /audio/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
    // Kanafuri and all other existing paths
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
